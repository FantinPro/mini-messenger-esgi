{
  "type": "module",
  "private": true,
  "scripts": {
    "start": "npm install && npm --workspace client run production && node ./build/server/server.js"
  },
  "workspaces": [
    "client",
    "server"
  ]
}
