import React from 'react';
import CounterError from '../error/CounterError';

export default function Admin () {

    return (
        <div>
            <h1>Admin</h1>
            <CounterError />
        </div>
    );
}